this关键字的使用：

1. 在set()方法中，当形参与属性重名时，使用this来标定属性
    public void setName(String name){
      this.name(属性）=name（形参）;
      }
2. this 可以调用的结构：成员变量，方法，构造器
3. this 的理解：
    当前对象（在方法中使用时）
    当前正在创建的对象（在构造器中使用时）

4. this 调用构造器；
    1. 格式："this（形参列表）"
    2. 我们可以在类的构造器中，调用当前类中指定的其他构造器
    3. 要求："this（形参列表）"必须什么在当前构造器的首行
    4. 结论："this（形参列表）"在构造器中最多能申明一个
    5. 如果一个类中申明了n个构造器，则最多有n-1个构造器可以申明有"this（形参列表）"的结果


